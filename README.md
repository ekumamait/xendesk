# XenDesk

Internal customer support & ticketing system for XenFi Systems.

> **`main` is the production branch** — application code is merged here on
> release; active development happens on `dev`. See `dev` for full setup docs.
